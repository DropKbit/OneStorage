# v0.31 合并队列验证

开发中的验证记录；尚未作为生产完成声明。

- 类型检查与 331 项测试通过。新增 10 项队列测试和 1 项真实 Repository 类集成测试，覆盖精确候选发布、FIFO/失败阻塞、基线更新后重新批准、配置变化取消 CI、权限和生命周期失效、D1 响应丢失恢复、直接写入和无关 CI 不得绕过门禁、目标配置选择、快进/压缩/无操作合并。
- Repository 集成测试在 Git 引用发布后注入 D1 投影失败，重建 DO 实例并运行 alarm，验证相同 SHA、MR/队列状态恢复及一次审计；同时验证内部 Fork 引用读取不等待写锁。
- 本地端到端通过 96 次 API/浏览器响应断言（含状态轮询），真实执行三个候选 CI：两个成功合并、一个失败并从移动端取消。源文件读取、Issue 自动关闭、旧批准失效、原生 Git clone/main SHA 与严格 fsck 均通过。桌面和移动端截图已检查，无页面脚本错误或移动端横向溢出。
- 本地前两次尝试为验收脚本错误：私有队列匿名访问应为 401，原始文件响应应按内容类型读取。分别清理夹具后修正重跑，没有为通过测试修改这些服务端行为。
- 本地和生产已应用 `0025_merge_queue.sql`。生产迁移前导出 D1 并独立恢复：原有 68 张表记录数保持不变，完整性检查通过、外键违例为零。此验证针对 D1，不等于 R2/DO 的完整灾备演练。
- Worker 部署、线上验收与源码同步待执行。

持续更新本记录，阶段交付不表示完整 GitLab/Gogs 目标完成。
