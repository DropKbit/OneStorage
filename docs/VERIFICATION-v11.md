# v0.11 验证记录

2026-09-08，项目转移与重命名。

- TypeScript 和 130 个单元测试通过。新增覆盖跨空间继承角色变化、显式成员保留、UUID/协作/CI 历史保留、租约与连接撤销、应用关闭、目标冲突全部回滚、源/目标事务内撤权、归档状态保留、历史路径保留/回迁、同空间重命名保留连接，以及请求级 D1 包装器的 RETURNING、batch、不可变绑定、旧版本拒绝与整体回滚。
- 本地转移验收 79 项通过：个人到团队、团队间、归档项目转移、重命名；原生 Git clone/fetch/push/fsck；LFS 保留；旧路径与 JWT、空间成员、只读 PAT 权限；真实隔离 JS 云 CI 生成部署版本。临时项目和空间已清理，测试用户停用并撤销凭据。
- 本地既有归档 63 项、Issue 工作流 62 项、协作/隔离云 CI 81 项回归通过；归档和 Issue 临时项目已清理。生产转移验收 83 项、归档回归 61 项通过，包含实际云端应用转移前可访问、转移后返回 404；生产临时项目、工作空间及应用已清理，测试用户停用并撤销凭据。完整浏览器交互仍待 Mac 解锁验收。

- 首次生产部署 13 个资源及源码归档逐文件哈希/MIME/版本缓存验证通过，健康接口 0.11.0，Cubelink 正常。最终部署再使用 `npm run verify:release` 核对。

总目标保持进行中，参见 [持续能力矩阵](CLOUD-NATIVE-v05.md)。
